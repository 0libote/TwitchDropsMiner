/**
 * Version constants. Mirror `fork_version.py` (`__version__`) and upstream
 * `version.py` — keep all three in sync on release bumps.
 */
export const FORK_VERSION = "0.1.0-alpha.5";
export const UPSTREAM_VERSION = "16.dev";
